import { createContext } from "react";

export const AddContext = createContext(true);

// function AddContextProvider({ children }) {
//   return <AddContext.Provider value={} >{children}</AddContext.Provider>;
// }

export default AddContext;
